#pragma once

#include "game/GameIncludes.h"
#include "game/GameTypes.h"

#include "game/GameConstants.h"
#include "math/mathUtils.h"

/**
 * @brief The function which runs when the Ceiling 1 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnCeiling1Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Ceiling 2 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnCeiling2Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Ceiling 3 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnCeiling3Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Ceiling 4 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnCeiling4Start(GameObject* gameObject);