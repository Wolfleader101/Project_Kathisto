#pragma once

#include "game/GameIncludes.h"
#include "game/GameTypes.h"

#include "game/GameConstants.h"
#include "math/mathUtils.h"

/**
 * @brief The function which runs when the Wall 1 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall1Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 2 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall2Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 3 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall3Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 4 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall4Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 5 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall5Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 6 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall6Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 7 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall7Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 8 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall8Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 9 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall9Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 10 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall10Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 11 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall11Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 12 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall12Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 13 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall13Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 14 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall14Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 15 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall15Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 16 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall16Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 17 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall17Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Wall 18 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnWall18Start(GameObject* gameObject);