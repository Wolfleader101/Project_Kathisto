#pragma once

#include "game/GameIncludes.h"
#include "game/GameTypes.h"

#include "game/GameConstants.h"

#include "math/mathUtils.h"

/**
 * @brief The function which runs when the Floor 1 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnFloor1Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Floor 2 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnFloor2Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Floor 3 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnFloor3Start(GameObject* gameObject);
/**
 * @brief The function which runs when the Floor 4 object starts
 * @param GameObject* gameObject - Input game object
*/
OnStart OnFloor4Start(GameObject* gameObject);